#ifndef __PDA_CALENDAR_H__
#define __PDA_CALENDAR_H__

// PDA > Calendar: monthly grid with per-day events stored in calendar/events.txt.
// Nav: arrows = day/week, [] or Fn+←→ = month, Fn+↑↓ (;/.) = year.
void pdaCalendar();

#endif
