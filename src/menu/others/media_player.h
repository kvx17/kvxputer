#ifndef __MEDIA_PLAYER_H__
#define __MEDIA_PLAYER_H__

// Tools > Media Player: browse audio on storage and play it through the
// firmware's existing audio pipeline (musicPlayerUI). Requires a speaker.
void mediaPlayerApp();

#endif
